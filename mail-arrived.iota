rule "mail arrived.groovy"

	device accelerationSensor : accelerationSensor;
	input active : bool;
	input inactive : bool;
	output send : string;

	accelerationSensor.acceleration [ .~>currentPushNotification ];
	
	active == true;
	send ( "arrived mail" );

      | inactive == false;
	send ( "not arrived mail" );

end
