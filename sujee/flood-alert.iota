rule "Flood Alert!"

    device alarm : capability.waterSensor;
    output recipients : contact;
    output phone : phone;

    Time cron "0 * * * * * *";  // 60초마다 실행하는 것의 문제 (카네기 멜론 논문 참고)

        alarm.wet = "wet";      // 1초동안 이벤트 탐지 불가, 결국 해당 시점에 젖어 있는지만 확인
        send(alarm + " is wet, texting phone number");  // alreadySentSms 구현 불가

end