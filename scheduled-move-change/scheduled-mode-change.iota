rule "scheduled-mode-change.groovy"


    input newMode : mode;
    input time : time;
    output send : string;

    location.mode != newMode;

        send ( "Sensed mode : " + newMode )
        
end